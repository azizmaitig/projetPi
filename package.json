{
  "name": "parking-management-app",
  "version": "1.0.0",
  "description": "Parking space management application",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"cd frontend && npm start\" \"cd backend && node server.js\"",
    "install-deps": "npm install && cd frontend && npm install && cd ../backend && npm install"
  },
  "dependencies": {
    "concurrently": "^8.2.2",
    "framer-motion": "^12.4.7"
  }
}
